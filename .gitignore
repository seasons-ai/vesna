node_modules/
dist/
# Vesna's own working directory in this repository: config, specs, logs, worktrees.
.vesna/
*.log
.DS_Store

# Per-developer, not per-project: whose provider, whose learned permissions.

# Scratch for the plan-execution skill; the record is the git history.
.superpowers/

# npm pack output
*.tgz

vscode/dist/
vscode/node_modules/
*.vsix
