Closes #

## What this does

## How it was checked

CI does not run on a pull request by itself — you run the suite, and paste
the last three lines of `bun test` and the result of `bun run typecheck`
here. The maintainer runs CI on the request when it is ready to merge.

```
(paste here)
```

- [ ] An issue was opened first and is linked above
- [ ] `bun test` and `bun run typecheck` are green on my machine, output pasted above
- [ ] Everything in the diff is in English
- [ ] The commit message describes only what the diff does
